esphome:
  name: sendspin-mc-test
  project:
    name: fixtures.sendspin
    version: test-project

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

logger:

wifi:
  ap:

psram:
  mode: octal
  speed: 80MHz

external_components:
  - source:
      type: local
      path: ..
    components: [sendspin_mc]

# Images are exercised without a physical display in these build fixtures.
display: []
