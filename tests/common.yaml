esphome:
  name: sendspin-mc-test

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

logger:

wifi:
  ap:

psram:
  mode: octal
  speed: 80MHz

external_components:
  - source:
      type: local
      path: ..
    components: [sendspin_mc]
